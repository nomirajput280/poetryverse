# PoetryVerse Android wrapper

This is a lightweight Android WebView package for the live PoetryVerse Deno deployment.

Default URL: https://poetryverse.786.deno.net

Open the `android` folder in Android Studio or AndroidIDE and build a debug APK. The web app itself remains the source of truth, so website/API changes do not require rewriting the Android wrapper.
